import { SectionTitle } from "@/components/header/sectionTitle";

export const MySkills = () => {
  return (
    <div id="my-skills" className="w-full p-8 bg-sky-100">
      <SectionTitle text="My Skills" />
      <div className="text-center">
        <p>私の持つスキルを紹介します。</p>
      </div>
    </div>
  );
};
